[package]
name = "fakeedid"
version = "0.1.0"
edition = "2024"
license = "GPL-2.0-only OR GPL-3.0-only"

[dependencies]
uefi = { version = "0.36.1", features = ["panic_handler"] }

[[bin]]
name = "fakeedid"
test = false
